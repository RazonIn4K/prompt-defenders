# Environment Variables for Prompt Defenders

# =============================================================================
# Required for ALL environments
# =============================================================================

# Security: Hash salt for input hashing (change in production!)
HASH_SALT=your-secure-random-salt-here-change-in-production

# =============================================================================
# Required for Datadog RUM (L5 Analytics Tool)
# =============================================================================

# Datadog Application ID
NEXT_PUBLIC_DD_APP_ID=your-datadog-app-id

# Datadog Client Token
NEXT_PUBLIC_DD_CLIENT_TOKEN=your-datadog-client-token

# Environment (development, staging, production)
NEXT_PUBLIC_ENV=development

# Commit SHA for version tracking (usually set by CI/CD)
NEXT_PUBLIC_COMMIT_SHA=local-dev

# =============================================================================
# Optional: Upstash Redis (for production rate limiting & async queue)
# =============================================================================
# If not set, falls back to in-memory token bucket (not suitable for production)

UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# =============================================================================
# API Key Authentication (Production Only - Phase 2)
# =============================================================================
# Comma-separated list of valid API keys
# Optional in development (permissive), required in production
# Clients must provide X-API-Key header with one of these keys in production

API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# =============================================================================
# Notes
# =============================================================================
#
# 1. NEVER commit this file with real values
# 2. Copy to .env.local for local development
# 3. Set these in your deployment platform (Vercel, AWS, etc.)
# 4. See docs/SECURITY.md for security best practices
# 5. See docs/Go-Live-Gate.md for production requirements
#
